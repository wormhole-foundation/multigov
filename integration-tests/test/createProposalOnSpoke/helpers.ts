export const createProposalOnSpoke = async () => {
  // TODO: Add the rest of the logic to create a proposal on spoke
  return 1n;
};

export const dispatchProposalToHub = async (proposalId: bigint) => {
  console.log('🦄 ~ dispatchProposalToHub ~ proposalId:', proposalId);
  // TODO: Add the rest of the logic to dispatch a proposal to the Hub
};

export const addProposalToSpoke = async (proposalId: bigint) => {
  console.log('🦄 ~ addProposalToSpoke ~ proposalId:', proposalId);
  // TODO: Add the rest of the logic to add a proposal to spoke
};
